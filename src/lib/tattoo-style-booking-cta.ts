/** Primary booking CTA copy for tattoo style detail pages. */
function tattooStyleBookingCtaLabel(styleTitle: string): string {
  return `Book ${styleTitle}`;
}

export { tattooStyleBookingCtaLabel };
