export interface RegionHomepageAuthorityConfig {
  eyebrow: string;
  heading: string;
  /**
   * Single institutional body paragraph (centered in the Authority band).
   * Wrap key phrases in `**double asterisks**` for accent emphasis.
   */
  description: string;
  ctaLabel: string;
  ctaHref: string;
}
